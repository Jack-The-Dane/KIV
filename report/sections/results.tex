\section{Results}
The grid search algorithm searched through $1152$ different combinations of hyper-parameters such as kernel sizes in the CNN and the hidden size in the LSTM. Determined by the F1-score the best 5 models had these parameters and F1-score:
\begin{table}
    \centering
    \begin{tabular}{c|c|c|c|c|c}
        Parameter    & Model 1 & Model 2 & Model 3 & Model 4 & Model 5 \\
        kernel\_size1 & 13     & 5       & 11      & 11      & 9       \\
        kernel\_size2 & 5      & 9       & 7       & 7       & 5       \\
        cnn\_internal & 32     & 64      & 128     & 64      & 64      \\
        cnn\_out      & 128    & 64      & 256     & 128     & 128     \\
        hidden\_size  & 128    & 64      & 128     & 256     & 64      \\
        num\_layers   & 2      & 2       & 3       & 1       & 2       \\
        dropout       & 0.5    & 0.5     & 0.5     & 0.5     & 0.2     \\
        learning\_rate& 0.001  & 0.001   & 0.001   & 0.001   & 0.001   \\
        weight\_decay & 0.0001 & 0.0001  & 0.0001  & 0.0001  & 0.0001  \\
        F1-score      & 0.9913 & 0.9912  & 0.9912  & 0.9891  & 0.9891 
    \end{tabular}
    \caption{Table showing the 5 best models, determined by F1-score, found by the grid search algorithm. All models were validated using hold-out validation with 10\% of data being used for the final evaluation of each model, 20\% being used for validation during training, and 70\% being used for training.}
    \label{tab:gridsearch}
\end{table}
The five models mentioned in \cref{tab:gridsearch} were then validated using 5-fold cross validation, eliminating any possible advantages that might result from a "lucky" train/test split in the hold-out validation. After cross validation, model 2 proved to be the best model with performance metrics as shown in \cref{tab:bestmodel}, averaged out over all 5 folds.

\begin{table}
    \centering
    \begin{tabular}{c c c c}
        F1-score & Precision & Recall & Accuracy \\
        0.9895   & 0.9652    & 0.9478 & 0.9570
    \end{tabular}
    \caption{Performance metrics of model 2 from \cref{tab:gridsearch}. The metrics are calculated after every fold in a 5-fold cross validation, and then the averages over all folds are shown here.}
    \label{tab:bestmodel}
\end{table}

\begin{figure}
    \centering
    \begin{subfigure}{0.45\textwidth}
        \includegraphics[width=\linewidth]{figures/EEG_confusion.png}
    \end{subfigure}
    \begin{subfigure}{0.45\textwidth}
        \includegraphics[width=\linewidth]{figures/eeg_loss.png}
    \end{subfigure}
\end{figure}

Summarize model performance, loss, accuracy (vs epoch) plots, confusion matrix, and any key visualizations. Make sure you compare your results with at least one of the SOTA algorithm. You are allowed to compare your results with your other group member results (provided you are implementing different algorithms/models). Please make sure that the plots should have x and y labels along with their units (if any).

About 2 pages.